<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lagou.dao.RoleMapper">

    <!--查询所有角色-->
    <select id="findAllRole" parameterType="com.lagou.domain.Role" resultType="com.lagou.domain.Role"  >

        select * from roles
        <where>
            <if test="name != null and name != '' ">
                and name =#{name}
            </if>
        </where>

    </select>

    <!--    根据角色id  查询对应的 关联信息的id  -->
    <select id="findMenuByRoleId" resultType="int" parameterType="int">
        SELECT m.`id`
            FROM roles r  INNER JOIN role_menu_relation rm ON r.`id` = rm.`role_id`
	                      INNER JOIN menu m ON m.`id` = rm.`menu_id` WHERE r.id = #{id}
    </select>


    <delete id="deleteRoleContentMenu" parameterType="int">
        delete  from role_menu_relation where role_id = #{rid}
    </delete>

    <!--为角色分配菜单-->
    <insert id="roleContentMenu" parameterType="com.lagou.domain.Role_menu_relation">
        insert into role_menu_relation values(null,#{menuId},#{roleId},#{createdTime},#{updatedTime},#{createdBy},#{updatedby})
    </insert>

    <!-- 删除角色   -->
    <delete id="deleteRole" parameterType="int" >
        delete from roles where id = #{roleid}
    </delete>





    <!--    根据角色id 查询资源分类信息  -->
    <select id="findResourceCategoryByRoleId" parameterType="int" resultType="com.lagou.domain.ResourceCategory">

            SELECT DISTINCT rc.*
            FROM resource_category rc INNER JOIN resource r ON r.`category_id` = rc.`id`
			                          INNER JOIN role_resource_relation rr ON rr.`resource_id` = r.`id`
			WHERE rr.`role_id` = #{id}

    </select>

    <!--    根据 资源分类的 id 查询对于资源信息    -->
    <select id="findResourceByCategoryId" parameterType="int" resultType="com.lagou.domain.Resource" >
        SELECT * FROM resource WHERE category_id = #{id}
    </select>


    <!-- 清空 角色id 对应的资源中间表    -->
    <delete id="deleteRoleContextResource" parameterType="int">
        delete from role_resource_relation where role_id =#{id}
    </delete>

    <!-- 为角色分配资源信息   -->
    <insert id="roleContextResource" parameterType="com.lagou.domain.RoleResourceRelation">
        insert into role_resource_relation (id, resource_id, role_id, created_time, updated_time, created_by, updated_by ) values(null, #{resourceId}, #{roleId}, #{createdTime}, #{updatedTime}, #{createdBy},  #{updatedBy})
    </insert>



</mapper>